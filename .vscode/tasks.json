{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-I/usr/include/eigen3",  // Add the path to Eigen headers -I/usr/include/eigen3
                "-fdiagnostics-color=always",
                "-g",
                //"${file}",
                "${workspaceFolder}/main.cpp",    // Main file
                "${workspaceFolder}/algorithms.cpp", // Additional file
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${workspaceFolder}/main"   // Output binary
            ],
            "options": {
                //"cwd": "${fileDirname}"
                "cwd": "${workspaceFolder}"     // Current directory: project folder
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
